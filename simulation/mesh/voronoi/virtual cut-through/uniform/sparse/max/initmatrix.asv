function [posmatrix] = initmatrix(m,n,pos,S)

sizes = size(S);

posmatrix = zeros(m,n);

sizepos = size(pos);

for i = 1: sizepos(1)
    
    x = pos(i,1);
    y = pos(i,2);
    
    for dis = 0:S(i)   
        [dpos]= dispos(x,y,dis,m,n);
        
        sizedpos = size(dpos);
        
        
        
    end

end

end